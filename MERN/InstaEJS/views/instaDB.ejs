<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InstaDB</title>
   <!--accessing css file no /public required as we have already declared it in index app.use(express.static())-->
 <link rel="stylesheet" href="/style.css"/>
 
</head>
<body>
    <div>
      <h2>This page belongs to @<%= data.name %></h2>
      <p>Followers : <%= data.followers %></p>
      <p>Following : <%= data.following %></p>
      <button>Follow</button>
      <button>Message</button>
    </div>

    <hr>
    <% for(let post of data.posts){%>
        <!-- converting string to variable using <%= %> tags -->
    <img src = "<%=post.image%>" alt=" some image"> 
    <p>
        Likes: <%= post.likes %> 
        <br>
        comments : <%= post.comments %>
    </p>
    
    
    <%}%>
    <%- include("includes/footer.ejs")%>
  
</body>
<script src="/app.js"></script><!--Script tag needs to added at last-->
</html>