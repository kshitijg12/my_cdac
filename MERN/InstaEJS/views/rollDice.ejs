<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roll Dice</title>
</head>
<body>
    <h1>Dice gave value :<%= Math.floor(Math.random() * 6)+1 %></h1>

    <!-- We can also pass value from index.js-->
    <h1>Dice gave value :<%= diceVal %></h1>

<!-- Writing a logical if else block  <%  %>  does not return value-->
<!-- Only JS code comes under <% %> tags html has to be written outside surrounded by JS code-->
<% if(diceVal==6){ %>
    <h2>Great ! , Now you can roll dice again</h2>
<% } %>

<%- include("includes/footer.ejs")%>
</body>
</html>